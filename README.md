# CursoDownloader — Motor

Motor atualizável do app CursoDownloader (a pasta `core/`).
As máquinas apontam a aba **Atualização** para o link "raw" desta pasta e baixam correções sem reinstalar o app.

Ao corrigir uma plataforma: suba a versão em `motor.json` e faça push aqui.
